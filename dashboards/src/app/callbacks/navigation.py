from dash import callback, Input, Output

def register_navigation_callbacks(app):
    """Registrar callbacks de navegación"""
    # Los callbacks de navegación ahora se manejan en auth.py
    # Este archivo se mantiene para compatibilidad pero no registra callbacks
    pass
